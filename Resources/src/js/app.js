import React from 'react'
import { FormContainer, ListContainer, LeafletContainer } from './containers'

const app = () => (
    <div className="asdoria-relay">
        <FormContainer />
        <ListContainer />
        <LeafletContainer />
    </div>
);

export default app
